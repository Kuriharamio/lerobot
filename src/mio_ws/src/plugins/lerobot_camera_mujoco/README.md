# lerobot_camera_mujoco

MuJoCo camera plugin for LeRobot.

This package provides:

- `MujocoCameraConfig` (camera config with `type="mujoco"`)
- `MujocoCamera` (renders RGB frames from a named MuJoCo `<camera>` in a model)

